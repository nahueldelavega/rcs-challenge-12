// Area del triangulo

let base = 20;
let height = 10;


let triangleArea = (base*height/2);
console.log(triangleArea);

// Area del circulo

let circleRadio = 50;

let circleArea = (Math.PI*(Math.pow(circleRadio,2)))
console.log (Math.round(circleArea));